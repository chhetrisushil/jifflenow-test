# jifflenow-test

### View the output
* Run `npm install`
* Run `npm run build`
* Open `index.html` in chrome

### Contribution
* Run `npm install`
* Run `npm run build:watch` (starts webpack in watch mode for development)
* Open `index.html` in chrome
* Start contributing
